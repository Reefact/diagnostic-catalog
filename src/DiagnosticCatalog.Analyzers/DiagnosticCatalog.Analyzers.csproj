<Project Sdk="Microsoft.NET.Sdk">

  <!--
    netstandard2.0 and nothing else, and not for the reason the shipped libraries have. An analyzer is
    loaded into the host compiler's process — VBCSCompiler, Visual Studio, Rider — and that host reads
    only netstandard2.0 analyzer assemblies. Multi-targeting here would produce assemblies no host ever
    loads. ADR-0001 governs the libraries' floor; this one is Roslyn's own requirement.
  -->
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <AssemblyName>DiagnosticCatalog.Analyzers</AssemblyName>
    <RootNamespace>DiagnosticCatalog.Analyzers</RootNamespace>
    <!-- Nobody calls these types: the compiler instantiates them by reflection. XML docs would be
         published for an API no consumer writes against, and would turn every missing <summary> on an
         internal helper into CS1591 noise. The reasoning that matters lives in comments, next to the
         code. -->
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
    <!-- Turns on the analyzer-authoring rules (RS1xxx) and release tracking (RS2008). Without it, a
         diagnostic declared in code but absent from AnalyzerReleases.*.md ships unrecorded. -->
    <EnforceExtendedAnalyzerRules>true</EnforceExtendedAnalyzerRules>
  </PropertyGroup>

  <!--
    Declares this project to the catalogue generator's solution mode, which reads the projects that
    say so and never infers which ones they are. The declaration is the whole membership, exactly as <ReleaseTrain>
    above decides which train a project ships on and never a list kept somewhere else.

    Not derived from EnforceExtendedAnalyzerRules next to it, close as the two look. That one says
    "check this project by the analyzer-authoring rules"; DiagnosticCatalog.CodeFixes sets it too and
    declares no DiagnosticDescriptor at all. What a project is checked by and what it produces are
    different facts, and a catalogue built on the wrong one is short of rules with nothing to say so.
  -->
  <PropertyGroup>
    <ProducesDiagnosticRules>true</ProducesDiagnosticRules>
  </PropertyGroup>

  <!--
    NO <ReleaseTrain>, and no package identity of its own: this assembly is BUNDLED into the
    DiagnosticCatalog package rather than published beside it (ADR-0037). It is the same project shape
    DiagnosticCatalog.CodeFixes already has, and the one ADR-0007 blesses for code that is not its own
    package — tools/trains.sh leaves an untrained project alone by design.

    The reason is the release train. This analyzer and the foundation were both on `lib`, so they could
    never be released apart: same tag, same version, always. A second package therefore bought no
    versioning independence at all, and cost a second name a consumer had to discover — which is why
    thirteen catalogues shipped with nothing checking their consumers' suppressions. Every catalogue
    already depends on the foundation and is forbidden from hiding it, so folding the checks in there
    makes them arrive with any catalogue, declared nowhere.

    The consequence to remember, exactly as for the code fixes: nothing publishes this assembly on its
    own, so DiagnosticCatalog both orders its build and packs its output. See the ProjectReference
    there.
  -->
  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <!--
    Build order, not a code dependency. ReferenceOutputAssembly="false" means nothing here compiles
    against the code fixes — the two assemblies share source, not references — but the project is built
    before this one, so that DiagnosticCatalog can pack both after building either.
  -->
  <ItemGroup>
    <ProjectReference Include="..\DiagnosticCatalog.CodeFixes\DiagnosticCatalog.CodeFixes.csproj"
                      ReferenceOutputAssembly="false"
                      PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <!--
      THE LOAD CONTRACT — do not bump to match the central pin.

      Microsoft.CodeAnalysis.CSharp is held at $(RoslynFloorVersion) through a VersionOverride, because
      the version this assembly is compiled against becomes the minimum Roslyn able to load it. A
      higher one fails to load with CS8032 on older SDKs and IDEs, and an analyzer that fails to load
      reports nothing — which looks exactly like a codebase with no problems. The floor lives in
      Directory.Build.props and RoslynFloorTests asserts against it.

      The central pin exists for eng/CatalogGen, a build-time tool that constrains nobody. The
      VersionOverride is what lets the two coexist under Central Package Management. Its value is
      deliberately not repeated here: Dependabot moves it, and nothing checks a number kept in prose.
    -->
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" VersionOverride="$(RoslynFloorVersion)" PrivateAssets="all" />
    <!-- Authoring analyzers only, not part of the load contract, so free to track the newest version. -->
    <PackageReference Include="Microsoft.CodeAnalysis.Analyzers" PrivateAssets="all">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <!-- Surfaces the floor to RoslynFloorTests as assembly metadata, so the test asserts against the
         single value in Directory.Build.props rather than a second copy that could drift from it. -->
    <AssemblyMetadata Include="RoslynFloorVersion" Value="$(RoslynFloorVersion)" />
  </ItemGroup>

  <ItemGroup>
    <!-- Release tracking (RS2008): every declared diagnostic must appear here, verified at build time.
         Both files are required even while empty — their absence is itself an RS2008 failure. -->
    <AdditionalFiles Include="AnalyzerReleases.Shipped.md" />
    <AdditionalFiles Include="AnalyzerReleases.Unshipped.md" />
  </ItemGroup>

  <ItemGroup>
    <!-- The DCAT ids are the contract shipped inside the DiagnosticCatalog package, and the analyzer
         types are its public surface. Renaming either is a breaking change (CLAUDE.md), and
         RS0016/RS0017 make that a build failure rather than a review oversight. The tracking stays
         here, with the code it describes, rather than moving to the project that packs it. -->
    <PackageReference Include="Microsoft.CodeAnalysis.PublicApiAnalyzers" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Include="PublicAPI.Shipped.txt" />
    <AdditionalFiles Include="PublicAPI.Unshipped.txt" />
  </ItemGroup>

  <!--
    NO reference to DiagnosticCatalog, and that is deliberate rather than an omission. §7.2 requires
    the marker to be matched by its fully qualified metadata name, never by symbol identity, so the
    analyzer never needs the attribute TYPE — only its name. Referencing the foundation would tempt an
    implementation into the SymbolEqualityComparer shortcut that breaks a catalogue embedding its own
    attribute.

    Since ADR-0037 the dependency runs the other way — DiagnosticCatalog references this project, for
    build order, so that it can pack the output — which makes the omission here load-bearing rather
    than merely tidy: adding it back would close a project reference cycle and fail the build. Nothing
    else guards it. pack.sh's cross-train guard never did: this project declares no train at all now,
    and both were on `lib` before, so the guard skipped the reference either way.
  -->

</Project>
